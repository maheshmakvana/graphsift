## Code Statistics Plugin

Generates code statistics using cloc (Count Lines of Code).

**Usage**: Run with the path to a repository or directory.

**Returns**: File counts, line counts, and language breakdown by category.

**When to use**: When you need to understand the composition of a codebase.
